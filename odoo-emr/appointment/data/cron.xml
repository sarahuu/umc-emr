<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="cron_mark_missed_appointments" model="ir.cron">
        <field name="name">Mark Missed Appointments</field>
        <field name="model_id" ref="model_appointment_appointment"/>
        <field name="state">code</field>
        <field name="code">model._cron_mark_missed_appointments()</field>
        <field name="interval_number">10</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>

    <record id="ir_cron_archive_expired_slots" model="ir.cron">
        <field name="name">Archive Expired Time Slots</field>
        <field name="model_id" ref="model_appointment_available_slot"/>
        <field name="state">code</field>
        <field name="code">model._cron_archive_expired_slots()</field>
        <field name="active">True</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
    </record>
    <record id="ir_cron_update_provider_availability" model="ir.cron">
        <field name="name">Update Provider Availability</field>
        <field name="model_id" ref="appointment.model_emr_provider"/>
        <field name="state">code</field>
        <field name="code">model.update_provider_availability()</field>
        <field name="interval_number">5</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>
    
</odoo>
